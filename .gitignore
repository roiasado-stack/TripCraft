node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
.vscode/*
!.vscode/extensions.json
*.log

# Generated bundle for sharing the source — rebuildable, so not tracked.
tripcraft-codebase.zip

# Seeded design canvas — rebuilt from design/*.dc.html, so not tracked.
design/tripcraft-*.html
