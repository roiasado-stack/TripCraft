# רעיונות לאחר ההגשה

לפי כלל 8 ב-FINAL-PROJECT-BRIEF.md: אין פיצ'רים חדשים שלא ברשימת 16 הימים.
כל רעיון שעולה תוך כדי העבודה נרשם כאן ונדון אחרי ההגשה — לא נבנה עכשיו.

---

## חילוץ אוטומטי מאישורי הזמנה (טיסה + מלון) בתחילת בניית טיול

**תאריך:** 2026-09-03

**הרעיון:** להעלות אישור הזמנה (PDF/תמונה/מייל) שכולל כרטיסי טיסה ואישור מלון,
ולתת למודל לחלץ ממנו את הנתונים (תאריכים, שדות תעופה, שם מלון, תאריכי צ'ק-אין/אאוט
וכו') וממלא איתם את אשף יצירת הטיול — במקום הזנה ידנית.

**היתכנות:** גבוהה. זו למעשה אותה טכניקה שכבר קיימת בקוד היום לסריקת דרכונים
(`supabase/functions/generate/index.ts`, kind `"passports"`) — שליחת תמונה למודל
עם יכולת ראייה, בקשת JSON מובנה בחזרה, ומילוי שדות בטופס איתו.

**האתגר המרכזי:** אישורי הזמנה מגיעים בעשרות פורמטים שונים בין חברות תעופה
ואתרי הזמנה שונים, כך ששיעור ההצלחה בחילוץ משתנה. בדיוק כמו בסריקת הדרכונים —
חובה להציג את התוצאה לאישור/עריכה של המשתמש לפני שמירה, לא להזין ישירות בלי בדיקה.

**סטטוס:** לא בתוכנית. לדון אחרי ההגשה.

---

## הפיכת TripCraft ל-SaaS מולטי-טננטי (B2B2C)

**תאריך:** 2026-09-14

**הרעיון:** מסמך אסטרטגיה שקיבלתי (`saas_travel_architectural_summary.pdf`) מציע הפיכת
המוצר לפלטפורמת SaaS מלאה לסוכנויות נסיעות: מודל multi-tenancy (סוכנות → כמה סוכנים →
כל אחד עם לקוחות משלו), white-labeling מלא (לוגו/PDF/דומיין), מנוע parsing אוטומטי
לשוברי טיסה/מלון, אינטגרציית API לסטטוס טיסה חי (FlightAware/Flightradar24), התראות
Push עם geofencing, ומודלי תמחור (Subscription, Pay-per-Trip, Freemium) עם תשלומים
דרך Stripe.

**מצב הקוד היום מול הרעיון:**
- **Multi-tenancy — לא קיים.** כל שורה שייכת ל-`user_id` בודד; אין `tenant_id`. המעבר
  דורש שכתוב יסודי של הסכמה וה-RLS בכל טבלה כמעט — לא תוספת, אלא שינוי בסיס.
- **White-Labeling — קיים חלקית.** שם + צבע סוכנות כבר מוצגים בדף השיתוף
  (`SettingsPage.tsx`, `SharePage.tsx`), אבל בלי לוגו, PDF ממותג, או דומיין מותאם.
- **Parsing אוטומטי + API טיסות חי — לא קיים.** יש תקדים טכני עובד (סריקת דרכון עם
  AI vision, ב-`generate/index.ts` kind `"passports"`) שמראה שהדפוס ישים, אבל parsing
  של שוברי הזמנה ו-API חי (FlightAware וכו') זה בנייה מאפס.
- **Push + Geofencing, ותשלומים (Stripe/quotas) — לא קיימים בכלל.** אפס תשתית.

**היתכנות:** כיוון עסקי הגיוני *אם* יום אחד ירצו למכור את זה כמוצר לכמה סוכנויות, לא
רק לשימוש עצמי. אבל זה שינוי יסודי, לא אינקרמנטלי — כדאי לעשות אותו פעם אחת ובכוונה,
כי הוספת `tenant_id` בדיעבד למערכת שכל כולה בנויה על "בעלים יחיד" היא כואבת.

**סטטוס:** לא בתוכנית. לדון אחרי ההגשה.
