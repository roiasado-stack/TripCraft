node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
.vscode/*
!.vscode/extensions.json
*.log

# Generated bundle for sharing the source — rebuildable, so not tracked.
tripcraft-codebase.zip

# Seeded design canvas — rebuilt from design/*.dc.html, so not tracked.
design/tripcraft-*.html

# Playwright MCP scratch output (snapshots, console logs) — not project files.
.playwright-mcp/

.vercel
.env*

# Supabase CLI local link cache (project ref, pooler URL) — machine state, not source.
supabase/.temp/
